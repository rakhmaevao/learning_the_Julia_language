# Шаблон решения задачи 872. Leaf-Similar Trees
# Запуск тестов:  ./leet test 872

using LeetKit.Support

function (leaf_similar(root1::TreeNode{V}, root2::TreeNode{V})::Bool) where V
    # TODO: напишите своё решение
    error("Задача ещё не решена: leaf_similar")
end

function find_leaves!(t::TreeNode, leaves)
    # TODO: напишите своё решение
    error("Задача ещё не решена: find_leaves!")
end

function find_leaves!(::Nothing, leaves)
    # TODO: напишите своё решение
    error("Задача ещё не решена: find_leaves!")
end
